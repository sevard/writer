[project]
name = "writer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0",
    "google-cloud-secret-manager>=2.25.0",
    "gunicorn>=23.0.0",
    "whitenoise>=6.0",
]
